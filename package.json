{
  "name": "automation-xml-builder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browserify index.js -o build/bundle.js && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "connect": "^3.4.0",
    "react": "^0.14.0",
    "react-dom": "^0.14.0",
    "serve-static": "^1.10.0"
  },
  "devDependencies": {
    "babelify": "^6.3.0",
    "browserify": "^11.2.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
